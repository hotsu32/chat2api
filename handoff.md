# 协作交接

最后更新：2026-04-17
负责人：主调度线

## 使用规则

- 每条工作线开始前先读 `memory.md` 与本文件。
- 任何工作线只修改自己认领的文件；跨线修改前先在本文件登记。
- 不记录真实 Token、Cookie、代理账号、私有域名或任何敏感信息。
- 优先做稳定性、兼容性、可观测性、资源释放、退避、缓存、限流与错误语义收敛。

## 当前工作线

### 线路 A：模型解析与模型校验

- 负责人：现有工作线
- 目标：收敛模型解析、缓存上游模型列表、统一 `model_not_found` 语义
- 主要文件：`api/models.py`、`chatgpt/ChatService.py`
- 进入条件：修改前先确认 `/v1/models` 兼容面不会被破坏
- 交付标准：
  - 未登录默认模型路径稳定
  - `gizmo` 路径不回归
  - 未知模型名、`CHECK_MODEL=true` 行为可解释

### 线路 B：OpenAI 兼容 API

- 负责人：现有工作线
- 目标：收敛 `/v1/models`、`/v1/chat/completions` 的错误处理与响应形态
- 主要文件：`api/chat2api.py`
- 依赖：线路 A
- 交付标准：
  - 资源释放明确
  - `JSONResponse` / `StreamingResponse` 路径一致
  - `/v1/models` 返回结构固定

### 线路 C：网关反代稳定性

- 负责人：现有工作线
- 目标：统一上游异常到网关错误语义，补足可观测性
- 主要文件：`gateway/reverseProxy.py`、`gateway/backend.py`
- 依赖：线路 D
- 交付标准：
  - 上游失败时错误码稳定
  - 客户端总能关闭
  - 日志能定位到路径与失败类型

### 线路 D：重试与低风险运行

- 负责人：现有工作线
- 目标：控制重试范围、加入退避、避免雪崩式重复请求
- 主要文件：`utils/retry.py`、`chatgpt/chatLimit.py`
- 依赖：线路 B、线路 C
- 交付标准：
  - 只对可重试错误重试
  - 退避参数可配置
  - 不放大 4xx 语义错误

### 线路 E：主调度线新增

- 负责人：主调度线
- 目标：统一任务优先级、合并交付、补充共享记忆、做安全边界把控
- 主要文件：`memory.md`、`handoff.md`
- 交付标准：
  - 每条线边界清晰
  - 交付顺序明确
  - 风险项被记录

### 线路 F：SaaS 用户 seed 轮换（跨界，需号池线配合）

- 负责人：**待认领 —— 需号池线主导**（SaaS 线只能改自己这半边，单独做不成）
- 提出日期：2026-09-12，来源：SaaS 批次 A 安全评审
- 目标：让「泄露的 seed」可以被作废并换新，且换新不丢用户的会话历史

- 问题陈述：
  - `user_auth.seed` 是用户进入镜像的唯一凭据，且**终身不变**。
  - 一旦泄露（转发链接、浏览器历史、日志、共享设备），当前**没有任何补救手段**：
    改密码不影响 seed，退出登录不影响 seed，客服也没有可执行的操作。
  - 与批次 A 的 `pw_version` 对照：密码这条路已经能救回被盗账号了，
    seed 这条路还是敞开的 —— 两条入口的安全等级不对齐，等于短板决定水位。

- 为什么 SaaS 线做不了：
  换 seed 不只是 `UPDATE user_auth SET seed = ?`。旧 seed 在 `globals.seed_map`
  里挂着 `{token, plan_type, conversations}`，直接改会：
  1. 让用户的会话列表凭空消失（新 seed 是空的）；
  2. 在 `seed_map` 里留下一条永不回收的孤儿记录，旧 seed **仍然能用** ——
     等于「轮换」了个寂寞，泄露的凭据照样有效。
  `seed_map` 的语义与生命周期归号池线，SaaS 线不得擅改。

- 需要号池线提供的接口（两个，签名可议）：
  1. `rebind_seed(old_seed, new_seed) -> bool`
     把 `seed_map[old]` 的挂载整体迁到 `new`，并**删除** `old` 使其立即失效。
     要求原子：迁一半失败必须回滚，不能出现两个 seed 都半可用。
  2. `drop_seed(seed) -> None`
     注销 / 删号时回收挂载，避免 `seed_map` 只增不减。

- SaaS 线届时负责的那一半：
  - `utils/store.py` 写新 seed（复用现有 `upsert_user_auth`）
  - `/settings` 加「重置访问链接」入口 + 二次确认（要说清「旧链接立刻失效」）
  - 走改密同款的限流桶，避免被刷
  - e2e：旧 seed 立刻 401、新 seed 能进、会话历史仍在

- 交付标准：
  - 旧 seed 在轮换后**立刻**不可用（不是等缓存过期）
  - 会话历史不丢
  - `seed_map` 无孤儿条目（轮换 N 次后条目数不增长）
  - 并发轮换不产生两个可用 seed

- 优先级：中。不阻塞批次 A 上线；但在「付费用户开始转发链接」之后会变成投诉源，
  建议排在号池线当前工作之后的第一档。

## 当前优先级

1. 先稳定 A + B：确保模型解析与 `/v1/models` 不互相打架
2. 再稳定 C + D：确保 502/超时/瞬时 5xx 不触发雪崩重试
3. 最后处理 E：把回归结果和遗留风险回写到 `memory.md`

## 主调度线判定标准

- 允许推进：
  - 缓存、队列、限流、退避、熔断、日志、指标、错误码收敛
  - 兼容性修复、资源释放、重复逻辑收敛
  - 文档、配置、部署与运行时保护

## 回归清单

- A/B 联合：
  - `/v1/chat/completions`
  - `/v1/models`
  - 未登录默认模型
  - `gizmo` 模型
  - 未知模型名
- C/D 联合：
  - 上游超时
  - 上游 502
  - 上游瞬时 500/503
  - 非可重试 4xx
  - 客户端关闭与日志输出
